{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "dockerfile"
  },
  "deploy": {
    "startCommand": "bash -c 'gunicorn --bind 0.0.0.0:${PORT:-8000} app:app'",
    "port": 8000
  }
}